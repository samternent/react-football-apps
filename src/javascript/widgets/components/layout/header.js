/** @jsx React.DOM */
var React = require('react');


var ScorePicker =
  React.createClass({
    render: function() {
      return React.DOM.div( {
          className: 'header'
        });
    }
  });

module.exports = ScorePicker;
